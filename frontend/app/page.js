export default function Home() {
  return (
    <main
      style={{
        maxWidth: 1100,
        margin: "40px auto",
        padding: "0 20px",
        fontFamily: "system-ui",
      }}
    >
      <h1 style={{ marginTop: 0, marginBottom: 24 }}>Convenio</h1>
      <p>Frontend is running.</p>
      <p>Use the navigation bar at the top.</p>
    </main>
  );
}